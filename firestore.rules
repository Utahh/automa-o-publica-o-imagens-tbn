rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Imóveis: leitura pública, escrita apenas via Admin SDK (script)
    match /imoveis/{docId} {
      allow read: if true;
      allow write: if false;
    }
  }
}
